/**
 * Functions that refelects the application behavior.
 *
 * This is typically the steps that are needed to make in order
 * to provide user functionality.
 */

function loadStats() {}

export default function startApplicationi() {
  loadStats();
}
